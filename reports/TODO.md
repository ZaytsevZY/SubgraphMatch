# Report TODO

## Identity and Metadata

- Author names and emails updated in `reports/main.tex`.
- Decide whether to keep `\setcopyright{none}` or update it to the final required ACM-compatible metadata.
- Confirm whether the course submission expects student IDs in the report front matter.

## Report Content

- Expand `Literature Review and Taxonomy` with more references and a clearer taxonomy figure/table.
- Add a short subsection in `Introduction` on project scope and course-specific goals.
- Add a short explanation in `Experimental Setup` on why `WordNet` and `Patents` are treated as boundary datasets.
- Division of labor updated with the final agreed roles.
- Add a short `Reflection` paragraph if the final report format expects it to be separate from `Conclusion`.

## References

- Verify all bibliography entries in `reports/references.bib`.
- Add any missing references cited in the literature-review section.
- Check DOI / venue / year fields for consistency.

## Figures and Tables

- Decide whether `results/tables/final_report_main_table.md` should be converted into a LaTeX-native table file.
- Add at least one dataset summary table to the report.
- Add at least one ablation figure or simplified visualization if time permits.

## Compilation and Submission

- Compile `reports/main.tex` locally with a LaTeX environment.
- Fix any formatting, citation, or overfull-box issues after compilation.
- Check that all referenced files are included in the final submission package.
- Confirm that AI-assistance records are preserved for submission.
